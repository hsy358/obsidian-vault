# 灵·洞本体工作站（Ontology Studio）— 凭据【脱敏版，可入库】

> **敏感原文**：已迁移到 `/root/.hermes/vault-credentials/ontology-studio.credentials`（chmod 600，gitignored）
> **部署时间**：2026-08-03
> **部署位置**：服务器 101.33.212.119
> **URL**：http://101.33.212.119/studio/

## 账号 1：演示管理员（对齐官方体验站 admin/admin）

> ⚠️ 重要：密码必须 15+ 字符（本体 v1.0.0 validatePassword 限制）
> Email: `admin@ontology.local`
> Password: `<见 /root/.hermes/vault-credentials/ontology-studio.credentials>`
> 角色: ORGANIZATION_ADMIN + SECURITY_AUDITOR

## 账号 2：主管理（bootstrap 创建）

> Email: `admin@hesiyan.local`
> Password: `<见 /root/.hermes/vault-credentials/ontology-studio.credentials>`
> 显示名: 何四燕
> 角色: ORGANIZATION_ADMIN + SECURITY_AUDITOR

## API 端点（公网）

- 健康检查: http://101.33.212.119/api/v1/health
- Ready: http://101.33.212.119/api/v1/health/ready
- 登录: POST /api/v1/auth/login
- 我: GET /api/v1/auth/me
- 项目列表: GET /api/v1/projects

## 架构

浏览器 → nginx 80 → caddy 19080 (HTTPS) → 本体 API 18080 / Web 13100

nginx 路径前缀:
- /api/* → caddy → api:18080
- /studio/* → caddy → web:13100
- /login, /account, /workspace, /solutions, /data-sources, /mappings, /quality, /releases, /tutorial, /work-items, /materializations, /ontology, /projects → caddy → web:13100
- /assets/, /brand/ → caddy → web:13100

## 永久规则（2026-08-03 教训）

1. Spring Security BCryptPasswordEncoder 跟 Python bcrypt 生成的 hash 不兼容（长密码 OK 短密码不 OK，原因可能是 validatePassword 强制）
2. 演示账号密码必须 15+ 字符（本体 v1.0.0 限制）
   - admin / admin 这种 5 字符密码 → 401
   - 16+ 字符密码 → 200 OK
3. 登录失败 5 次 15 分钟 throttle（identity_login_throttles 表）
   - `DELETE FROM identity_login_throttles;` 即时重置
4. vite base path = /studio/ + nginx regex 12 条路由兜底
5. CSRF 严格校验 Origin + Referer
   - ONTOLOGY_ALLOWED_ORIGINS 必须含公网 URL
6. caddy 容器如果 Created 不启动，手动 `docker start` 一次
