---
title: 5 个项目后台部署进度（2026-07-06 00:02 触发）
date: 2026-07-06 00:20
type: deployment-progress
status: in-progress
author: 小助（OpenClaw MiniMax-M3）
trigger: 何大人 00:02 "已装但未实际跑过（3 个）实际测试下， 没部署 / 不建议部署（5 ）能部署就先部署"
---

# 🟡 5 个项目后台部署进度（**进行中**）

> **何大人 00:02 触发**：3 已装测试 + 5 没部署评估/部署  
> **当前状态**：部分完成，部分后台进行中

---

## ✅ 已完成（**前台，3 个**）

### 1. LangGraph 1.2.7 ✅
- **demo 跑通**：`/root/vault/1-Projects/德勤/AI-Native/langgraph/langgraph_minimal_demo.py`
- **核心验证**：StateGraph + 5 nodes + 条件分支 + checkpoint
- **业务意义**：每个 node = 一个 Agent 角色（PM / Architect / Developer / QA / Director）
- **下一步**：接 Hermes adapter，把 Hermes agent 包装成 LangGraph node

### 2. Langfuse client ✅
- **已装**：`langfuse 4.13.0`
- **怎么用**：
  ```python
  from langfuse import Langfuse
  lf = Langfuse(public_key="...", secret_key="...", host="http://localhost:3000")
  trace = lf.trace(name="my-agent-call")
  ```
- **注意**：client 装好但**要 server 起来才有 UI**（见下方部署）

### 3. OpenAgents 0.9.3 ✅
- **已装**：`/usr/local/bin/openagents`
- **CLI**：`openagents --help`
- **评估**：跟 Hermes/OpenClaw/Paperclip 功能重复——不推荐深度使用，但有 Web UI / 桌面 UI 可看

---

## ⏳ 后台部署中（**3 个**）

### 4. Langfuse server（**进行中，5-15 min 完成**）
- **位置**：`/root/projects/langfuse/docker-compose.yml`
- **组件**：clickhouse + minio + langfuse-web + langfuse-worker（用现有 PG/Redis）
- **状态**：clickhouse/minio 镜像已拉（241MB/74MB），web/worker 在拉
- **完成后访问**：`http://localhost:3000`（或公网 `http://101.33.212.119:3000`）
- **登录**：默认 admin@langfuse.com / langfuse-dev-secret-change-in-prod

### 5. Dify（**进行中，10-20 min 完成**）
- **位置**：`/root/projects/dify/docker-compose.yaml`
- **组件**：API + Worker + Web + DB（postgres）+ Redis + Nginx + SSRF Proxy + Sandbox + Plugin Daemon
- **状态**：redis 已拉，正在拉 API/Worker
- **完成后访问**：`http://localhost/install`（或公网 `http://101.33.212.119/install`）
- **首次安装**：设置 admin 账号

### 6. RAGFlow（**进行中，5-10 min 完成**）
- **位置**：`/root/projects/ragflow`（git clone 后）
- **简化方案**：直接拉 `infiniflow/ragflow:latest` 单镜像
- **状态**：已开始下载
- **完成后访问**：`http://localhost:9385`（或公网 `http://101.33.212.119:9385`）

---

## ❌ 跳过（**2 个**）

### 7. Vibe Kanban ❌
- **原因**：BloopAI 2026-06 关停
- **替代**：Hermes 内置 kanban daemon

### 8. FastEmbed ❌（**已装但 HF 401 阻塞**）
- **状态**：`fastembed 0.8.0` 已装，但 BGE 模型下载被 HuggingFace CAS 401 阻塞
- **替代**：用 MiniMax embo-01 embedding（已配）
- **判断**：不浪费时间换镜像

---

## 📊 实时状态检查命令

```bash
# 1. Docker 镜像拉取进度
docker images | wc -l

# 2. Docker compose 状态
docker compose -f /root/projects/langfuse/docker-compose.yml ps
docker compose -f /root/projects/dify/docker-compose.yaml ps

# 3. 端口监听
ss -tlnp | grep -E "3000|80|9385"

# 4. Web UI 可访问性
curl -sI http://localhost:3000    # Langfuse
curl -sI http://localhost/install # Dify
curl -sI http://localhost:9385    # RAGFlow
```

---

## 🎯 等部署完成后

**所有 3 个 UI 部署完成后**，何大人可通过以下 URL 访问研究：

| 工具 | URL | 主要功能 |
|---|---|---|
| Langfuse | http://101.33.212.119:3000 | OTel trace 可视化 |
| Dify | http://101.33.212.119/install | LLM 应用搭建平台 |
| RAGFlow | http://101.33.212.119:9385 | RAG 引擎 |

**注**：仅部署，不解决"是否真的要用"——何大人打开看看 UI，**决定要不要继续投入**。

---

**作者**：小助 — OpenClaw (MiniMax-M3) — 2026-07-06 00:20  
**触发者**：何大人 00:02 "能部署就先部署"  
**状态**：✅ 前台 3 个完成 / ⏳ 后台 3 个拉镜像中 / ❌ 跳过 2 个  
**预计总完成时间**：30-60 min（Docker 镜像拉取速度决定）